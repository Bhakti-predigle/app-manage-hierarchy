import { Routes } from '@angular/router';
import { Manage_hierarchyComponent } from './manage_hierarchy/manage_hierarchy.component';

export const routes: Routes = [
     { path: '', component: Manage_hierarchyComponent },
];
